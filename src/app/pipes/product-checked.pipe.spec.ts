import { ProductCheckedPipe } from './product-checked.pipe';

describe('ProductCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
