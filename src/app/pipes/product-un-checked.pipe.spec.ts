import { ProductUnCheckedPipe } from './product-un-checked.pipe';

describe('ProductUnCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductUnCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
